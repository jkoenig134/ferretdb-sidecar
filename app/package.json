{
    "type": "module",
    "scripts": {
        "build": "tsc",
        "dev": "npm run build && docker compose up -d --build"
    },
    "dependencies": {
        "@js-soft/docdb-access-mongo": "^1.1.3",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "@types/express": "^4.17.17"
    }
}
